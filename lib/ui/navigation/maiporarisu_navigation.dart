import 'package:flutter/material.dart';

class MaiporarisuNavigation extends StatelessWidget {
  const MaiporarisuNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ページ切り替え'),
      ),
    );
  }
}
